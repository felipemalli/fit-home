export const exerciseIdPath = {
  put: {
    security: [{
      apiKeyAuth: []
    }],
    tags: ['Exercise'],
    summary: 'API for update the main properties of an exercise',
    parameters: [{
      in: 'path',
      name: 'exerciseId',
      required: true,
      schema: {
        type: 'string'
      }
    }],
    requestBody: {
      content: {
        'application/json': {
          schema: {
            $ref: '#/schemas/updateExerciseParams'
          }
        }
      }
    },
    responses: {
      200: {
        description: 'Success',
        content: {
          'application/json': {
            schema: {
              $ref: '#/schemas/exercise'
            }
          }
        }
      },
      403: {
        $ref: '#/components/forbidden'
      },
      404: {
        $ref: '#/components/notFound'
      },
      500: {
        $ref: '#/components/serverError'
      }
    }
  }
}
