{
  "name": "fit_home_api",
  "version": "1.4.0",
  "description": "Project for plan your own physical exercise schedule at home and help you to perform the exercises at the correct time.",
  "main": "index.js",
  "scripts": {
    "up": "npm run build && docker-compose up -d",
    "down": "docker-compose down --remove-orphans",
    "dev": "tsx watch src/main/server.ts",
    "up:dev": "docker-compose -f docker-compose.dev.yml up -d",
    "down:dev": "docker-compose -f docker-compose.dev.yml down --remove-orphans",
    "build": "rimraf dist/** && tsc -p tsconfig.production.json",
    "build:watch": "chokidar './src/**/*.ts' -c 'if [ '{event}' = 'unlink' ]; then rimraf dist/** && tsc -p tsconfig.production.json; else tsc -p tsconfig.production.json; fi' --silent",
    "build:dev": "rimraf dist/** && tsc -p tsconfig.development.json",
    "build:dev:watch": "chokidar './src/**/*.ts' -c 'if [ '{event}' = 'unlink' ]; then rimraf dist/** && tsc -p tsconfig.development.json; else tsc -p tsconfig.development.json; fi'",
    "debug": "nodemon -L --watch ./dist --inspect=0.0.0.0:9222 --nolazy ./dist/main/server.js",
    "api:terminal": "docker exec -it api-container /bin/bash",
    "mongo:terminal": "docker exec -it mongo-container /bin/bash",
    "test": "jest --passWithNoTests --silent --noStackTrace --runInBand",
    "test:verbose": "jest --passWithNoTests --runInBand",
    "test:unit": "npm run test -- --watch -c jest-unit-config.js",
    "test:integration": "npm run test -- --watch -c jest-integration-config.js",
    "test:staged": "npm run test --findRelatedTests",
    "test:ci": "npm run test -- --coverage"
  },
  "keywords": [],
  "author": "Felipe Vahia Malliagros",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.21.4",
    "@babel/preset-env": "^7.21.4",
    "@babel/preset-typescript": "^7.21.4",
    "@shelf/jest-mongodb": "^4.1.7",
    "@types/bcrypt": "^5.0.0",
    "@types/express": "^4.17.17",
    "@types/jest": "^29.5.0",
    "@types/jsonwebtoken": "^9.0.1",
    "@types/mongodb": "^4.0.7",
    "@types/node": "^18.15.11",
    "@types/supertest": "^2.0.12",
    "@types/validator": "^13.7.14",
    "@typescript-eslint/eslint-plugin": "^5.57.1",
    "babel-jest": "^29.5.0",
    "chokidar-cli": "^3.0.0",
    "eslint": "^8.37.0",
    "eslint-config-standard-with-typescript": "^23.0.0",
    "eslint-plugin-import": "^2.27.5",
    "eslint-plugin-n": "^15.7.0",
    "eslint-plugin-promise": "^6.1.1",
    "git-commit-msg-linter": "^4.9.4",
    "husky": "^8.0.3",
    "jest": "^29.5.0",
    "lint-staged": "^13.2.0",
    "nodemon": "^2.0.22",
    "rimraf": "^4.4.1",
    "supertest": "^6.3.3",
    "tsx": "^3.12.6",
    "typescript": "^5.0.3"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongodb": "^5.2.0",
    "validator": "^13.9.0"
  },
  "engines": {
    "node": "19.x"
  }
}
